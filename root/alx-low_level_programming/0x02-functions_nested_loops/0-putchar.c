#include "main.h"
/**
 * main - print the word "main"
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
char p[] = "main";

for (i = 0; i < 8; i++)
{
_putchar(p[i]);
}

_putchar('\n');
return (0);
}
